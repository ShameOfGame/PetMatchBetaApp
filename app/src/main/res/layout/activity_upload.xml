<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Button zum Auswählen eines Bildes -->
    <Button
        android:id="@+id/btnBildAuswaehlen"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Bild auswählen"/>

    <!-- EditText zur Eingabe des Beitragstitels -->
    <EditText
        android:id="@+id/etBeitragTitel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Beitrag Titel"
        android:layout_marginTop="16dp"/>

    <!-- EditText zur Eingabe des Beitragsinhalts -->
    <EditText
        android:id="@+id/etBeitragInhalt"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Beitrag Inhalt"
        android:layout_marginTop="16dp"/>

    <!-- Button zum Hochladen des Beitrags -->
    <Button
        android:id="@+id/btnBeitragHochladen"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Beitrag hochladen"
        android:layout_marginTop="16dp"/>

</LinearLayout>
